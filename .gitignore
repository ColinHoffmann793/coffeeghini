# dependencies
node_modules

# build output
dist

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# local env
*.local
.env
.env.*
!.env.example

# vite cache
.vite

# Capacitor native projects (regenerate locally with `npx cap add ios|android`)
# Remove these two lines if you want to version-control the native projects.
/ios
/android

# Personal documents kept in this folder — NEVER publish to the public repo
*.pptx
*.xlsx
*.docx
*.pdf
